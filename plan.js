front

accId as a part of user

user._id
user.username
user.accountId

save accId
  create a new default acc
  update a acc

if return selected is true then set accId to this id
  
use accId

select list
  add new txn
  add newMany txn

function sendUserInfo(accId) {
  res.json({
    accountId: accId,
  })
}

* mongo db test
find , add , del data

* sign up as a new client
  test new added portfolio
  check if curAccountId is set
  add new portfolio and update selected portfolio
  add new txn data check if accId
  import data check accId
create report basing year


